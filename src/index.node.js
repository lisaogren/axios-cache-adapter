import { setup, setupCache, serializeQuery } from './api'
import RedisStore from './redis'
import RedisDefaultStore from './redis-default'
import IoRedisStore from './ioredis'

export { setup, setupCache, serializeQuery, RedisStore, RedisDefaultStore, IoRedisStore }
export default { setup, setupCache, serializeQuery, RedisStore, RedisDefaultStore, IoRedisStore }
